name: gpt_rest #without hybrid search
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip:
      - fastapi
      - uvicorn
      - thefuzz
      - pycomfort>=0.0.15
      - polars>=0.19.11